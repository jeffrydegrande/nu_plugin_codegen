#!/usr/bin/env python

def do_something(name, email):
  print("Hello " + name + ", Email: " + email)

def main():
  {%- for item in items %}
    do_something("{{ item.name }}", "{{ item.email }}")
  {%- endfor %}

if __name__ == "__main__":
  main()
